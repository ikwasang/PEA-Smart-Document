# วิธีติดตั้ง PEA Smart Document บนเครื่องอื่น

## วิธีที่แนะนำ

1. คัดลอกโฟลเดอร์ `prototype` ทั้งโฟลเดอร์ไปยังเครื่องปลายทาง โดยให้มีไฟล์ `index.html`, `app.js`, `server.py`, `refine_pdf.py` และ `master-data.json` อยู่ครบ
2. ติดตั้ง Python 3.11 ขึ้นไปจาก python.org และเลือก **Add Python to PATH** ระหว่างติดตั้ง
3. เปิด PowerShell ในโฟลเดอร์ `prototype` แล้วติดตั้งไลบรารี:

```powershell
py -m pip install -r requirements.txt
```

4. เปิด `server.py` ด้วยคำสั่ง:

```powershell
py server.py
```

5. เปิดเบราว์เซอร์ไปที่ `http://127.0.0.1:8765/`

## ไฟล์ข้อมูล

`master-data.json` คือข้อมูลที่นำเข้าจาก Excel แล้ว จึงไม่จำเป็นต้องคัดลอกไฟล์ Excel ต้นฉบับเพื่อให้ระบบใช้งาน ถ้ามี Excel รุ่นใหม่ ให้นำเข้าบนเครื่องหลักแล้วคัดลอก `master-data.json` รุ่นใหม่ไปแทน

โฟลเดอร์ `local-data` มีร่างล่าสุดและรูปภาพจริง ควรคัดลอกเมื่อผู้ใช้ต้องการย้ายร่างไปเครื่องใหม่เท่านั้น และไม่ควรส่งต่อให้คนอื่นถ้ามีข้อมูลส่วนบุคคล

## การเปิดครั้งถัดไป

ดับเบิลคลิก `Open-PEA.cmd` ได้ ถ้าไฟล์นี้ยังชี้ไปยัง Python runtime ของเครื่องเดิม ให้ใช้คำสั่ง `py server.py` แทน หรือแก้ไฟล์ launcher ให้ใช้ Python ของเครื่องปลายทาง

รุ่นนี้ยังเก็บร่างล่าสุด 1 ชุดต่อเครื่องและเปิดรับเฉพาะเครื่องนั้น ไม่ควรเปิดพอร์ตหรือแชร์โฟลเดอร์ให้ผู้ใช้อื่นจนกว่าจะทำระบบหลายผู้ใช้และสิทธิ์การเข้าถึงเสร็จ
