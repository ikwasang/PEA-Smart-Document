# การอัปเดตข้อมูลกลาง

เปิด http://127.0.0.1:8765/admin หรือกด **จัดการข้อมูล** ที่หัวเว็บ ขณะเปิดโปรแกรมบนเครื่องนี้

1. เลือกประเภทข้อมูล: งบแฟ้มงาน รหัสบัญชี หรือรายชื่อบุคลากร
2. เลือกไฟล์ Excel `.xlsx` ที่มีรูปแบบเดียวกับไฟล์ต้นฉบับ ขนาดไม่เกิน 15 MB
3. กด **ตรวจไฟล์ก่อนอัปเดต** แล้วตรวจจำนวนเดิม จำนวนใหม่ ตัวอย่างข้อมูล และคำเตือน
4. กด **ยืนยันอัปเดตข้อมูล** เมื่อข้อมูลถูกต้อง
5. โหลดหน้ากรอกเอกสารใหม่เพื่อใช้รายการล่าสุด ทั้งจัดซื้อและจัดจ้างใช้ข้อมูลกลางชุดเดียวกัน

การนำเข้าจะแทนที่ข้อมูลเฉพาะประเภทที่เลือก รายการที่ไม่มีในไฟล์ใหม่จะหายจากตัวเลือก โดยไม่แก้ข้อมูลที่บันทึกในร่างเดิม ไฟล์งบแฟ้มงานรองรับหลายโครงข่ายต่อแฟ้มงาน รายชื่อบุคลากรใช้ร่วมกันสำหรับตัวเลือกบุคลากรในเอกสาร

ระบบตรวจไฟล์ก่อนบันทึกและสำรองข้อมูลเดิมทุกครั้งไว้ใน `local-data/master-backups` การตรวจไฟล์เพียงอย่างเดียวไม่เปลี่ยนข้อมูลจริง หากมีการอัปเดตข้อมูลระหว่างตรวจไฟล์กับยืนยัน ต้องตรวจไฟล์ใหม่

หน้านี้ใช้งานเฉพาะบนเครื่องที่รันโปรแกรม ยังไม่มีระบบเข้าสู่ระบบหรือแยกสิทธิ์ผู้ดูแล ก่อนขยายให้หลายคนใช้งานต้องเพิ่มการควบคุมสิทธิ์
